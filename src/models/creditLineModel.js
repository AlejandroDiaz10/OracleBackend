class CreditLine {
    constructor(id, creditLimit, currentBalance, updateDate) {
      this.id = id;
      this.creditLimit = creditLimit;
      this.currentBalance = currentBalance;
      this.updateDate = updateDate;
    }
  }
  
export default CreditLine;